from setuptools import setup, find_packages

setup(
    name='SVM_hinge',  # Replace 'SVM_hinge' with your package name
    version='0.1',     # Replace '0.1' with your package version
    packages=find_packages(),
    # Add any additional metadata or dependencies here
)
